# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: aca-instant-keyvault-pickup
metadata:
  template: aca-instant-keyvault-pickup@1.0.0
services:
  web:
    project: ./src
    language: java
    host: containerapp
    docker:
      path: Dockerfile
      context: .
      remoteBuild: true
